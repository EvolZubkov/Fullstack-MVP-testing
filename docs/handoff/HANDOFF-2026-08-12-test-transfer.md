# Передача работы: перенос теста между инсталляциями (.tbtest)

Дата: 2026-08-12. Ветка: `main`. Базовый коммит серии: `df2bb203 feat(home): все руководства
скачиваются из блока «Материалы»`. Последний коммит работы: `dcc44ead feat(transfer): роуты
трёхшагового импорта`. Статус: серверная часть готова к приёмке, интерфейса нет.

## 1. Задача

Повод: тест «Определение ведущего стиля человекоцентричного лидерства» перенесли из dev в
прод книгой Excel. В проде показатель напечатал `vdo` — сырой ключ шкалы вместо названия
стиля, диаграммы не было, встал дефолтный порог 70%.

Причина не в рендере: книга — формат СОДЕРЖАНИЯ и не несёт jsonb, в котором живут исходы
показателей, вводный блок, оформление, настройки отчёта, правило прохождения и конфигурация
экрана итогов. Импорт при этом не выдаёт ни одной ошибки.

Задача, поставленная владельцем: сделать перенос без потерь. По ходу требование расширено
(его же решением): форма выбора, какие части теста импортировать, и режим upsert/replace
там, где возможна потеря.

Документы: спека `docs/specs/prd-48/test-transfer.md`, план срезов
`docs/plans/2026-08-11-test-transfer-without-losses.md`, план реализации формы
`docs/plans/2026-08-12-prd48-selective-import.md`, эскиз формы
`.playwright-mcp/wf-transfer-import.html`.

## 2. Что сделано

Всё закоммичено девятью коммитами (`2844aa80`..`dcc44ead`). Задачи 1-5 плана реализации
закрыты, статусы и отступления записаны в самом плане.

1. **Скрипт расшивки прода** — `scripts/db/transfer-test-settings.ts`. ПРИМЕНЁН К ПРОДУ
   2026-08-12: 7 изменений, повторный прогон дал ноль расхождений. Временный, удаляется
   задачей 8.
2. **Формат `.tbtest` и выгрузка** — `server/services/test-transfer/{package,export,plan}.ts`,
   роут `GET /api/tests/:id/transfer`.
3. **Импорт-копия с сохранением идентификаторов** — `identity.ts`, `import.ts`,
   `storage/test-transfer-repository.ts` (`writeImportedTest`), роут
   `POST /api/tests/transfer`.
4. **Разбор без записи** — `inspect.ts` (`readTransferPackage`, `inspectPackage`,
   `inspectParsedPackage`) и `session-store.ts` (токен, TTL 30 минут, LRU 50, владелец).
5. **Ядро решений** — `diff.ts`: чистая `diffTransfer(пакет, снимок, опции)` -> операции
   create/update/delete. Пять частей, два режима, три политики темы, адаптивная
   конфигурация. Правило свободного имени темы вынесено в
   `shared/topics/naming.freeTopicName`.
6. **Применение** — `apply.ts` (`applyTransfer`, `TransferForbiddenError`), `target.ts`
   (`buildTargetSnapshot`), `applyTransferBatch` в репозитории переноса,
   `getAnsweredQuestionIds` в репозитории попыток и фасаде `IStorage`.
7. **Роуты трёхшагового импорта** — `POST /api/tests/transfer/{inspect,plan,apply}` в
   `server/routes/test-transfer.ts`.
8. **Тесты**: 56 модульных по переносу (7 файлов `server/__tests__/test-transfer-*.test.ts`)
   и 8 интеграционных на pglite (`tests/it/test-transfer.it.test.ts` — перенос в чистую базу,
   `tests/it/test-transfer-apply.it.test.ts` — применение, откат, отказ по правам).

ВНИМАНИЕ: в рабочей копии есть изменения ДРУГИХ сессий, к этой работе не относящиеся:
`client/src/features/tests/editor/sections/basic-settings-section.tsx` и его тесты,
`docs/dist/*.pdf`, `scripts/docs/build-docs-pdf.mjs`, `templates/certification/preview.html`.
Индекс общий — перед коммитом сверять `git diff --cached`.

## 3. Что НЕ сделано

- **Задача 6 — форма импорта** (`client/src/features/tests/transfer/`). Не начата: серверная
  часть шла первой по плану. Эскиз готов, чипы состояний темы в нём подобраны небрежно
  (в тёмной теме нейтральный сливается с фоном, `solid` кричит) — шаг 4 задачи 6.
- **Задача 7 — лист «Исходы показателей» в книге.** Не начата.
- **Задача 8 — закрытие долгов**: удалить скрипт расшивки, прогнать полный `npm run test:it`,
  убрать из dev тест-копию `f70eb383-28fe-4f51-abff-4bc4eb2013ac` и тему
  «Человекоцентричное лидерство (ЧИЛ) (импорт 2)», обновить спеку.
- **Полный прогон `npm run test:it` НЕ ВЫПОЛНЕН.** `tests/it/schema.sql` регенерирован, а
  прогнаны только два файла переноса. Владелец разрешения на полный набор не давал.
- **Четыре решения сверх спеки ждут подтверждения** — перечислены в задаче 3 плана
  реализации (частичная запись строки теста, удаление лишнего раздела приёмника, запасное
  сопоставление по ручке автора, новые идентификаторы вопросов при политике «новая тема»).

## 4. Как проверить

Модульные тесты переноса:

```bash
npm test -- server/__tests__/test-transfer-export.test.ts \
  server/__tests__/test-transfer-plan.test.ts \
  server/__tests__/test-transfer-roundtrip.test.ts \
  server/__tests__/test-transfer-completeness.test.ts \
  server/__tests__/test-transfer-identity.test.ts \
  server/__tests__/test-transfer-inspect.test.ts \
  server/__tests__/test-transfer-diff.test.ts
```

Ожидание: `Test Files 7 passed`, `Tests 56 passed`.

Интеграционные (pglite, по одному файлу):

```bash
npm run test:it -- tests/it/test-transfer.it.test.ts        # 3 passed
npm run test:it -- tests/it/test-transfer-apply.it.test.ts  # 5 passed
```

Живой круг по HTTP (нужна dev-БД в Docker, контейнер `test-builder-db`):

```bash
PORT=8097 npm run dev
# вход, выгрузка, три шага
curl -s -c c.txt -X POST http://localhost:8097/api/auth/login \
  -H 'Content-Type: application/json' -d '{"email":"...","password":"..."}'
curl -s -b c.txt "http://localhost:8097/api/tests/<testId>/transfer" -o pkg.tbtest
curl -s -b c.txt -X POST http://localhost:8097/api/tests/transfer/inspect -F "file=@pkg.tbtest"
curl -s -b c.txt -X POST http://localhost:8097/api/tests/transfer/plan \
  -H 'Content-Type: application/json' -d '{"token":"<токен>"}'
curl -s -b c.txt -X POST http://localhost:8097/api/tests/transfer/apply \
  -H 'Content-Type: application/json' -d '{"token":"<токен>"}'
```

Ожидание: выгрузка `200 application/zip`; разбор `200` с токеном и составом; план на
САМ ЖЕ тест — только `update` и ноль `delete`; применение `200`, повторный вызов плана с тем
же токеном — `404` (сессия удалена). Сильная проверка идемпотентности: выгрузить тест
повторно и сверить манифесты, отбросив `exportedAt` и `updatedAt`, — они обязаны совпасть.

Учётные данные приёмочной записи — в памяти проекта (`reference_dev_acceptance_account`),
в этот файл не выносятся. Строки подключения: dev — `.env`, прод —
`/srv/app/test_builder/env/.env` на сервере.

## 5. Состояние проверок

- Модульные тесты переноса: 56 зелёных (7 файлов), прогон 2026-08-12.
- Интеграционные: 3 + 5 зелёных, прогон 2026-08-12.
- `npx tsc --noEmit`: по всем файлам переноса чисто. В проекте ДО этой работы существуют две
  ошибки типов, к ней не относящиеся: `client/src/pages/author/analytics.tsx:1654` и
  `client/src/pages/author/test-analytics.tsx:458` (обе — `new Date(ReactNode)`).
- Живой HTTP-круг на dev выполнен 2026-08-12 на тест-копии `f70eb383-…`: разбор дал
  14 вопросов, 4 шкалы, 56 вкладов, состояние темы `existing`; план на самого себя — 86
  обновлений и ноль удалений; политика «создать новую тему» — создание темы и 14 вопросов;
  применение прошло, повторная выгрузка совпала с исходной байт в байт.
- Полный `npm test` и полный `npm run test:it` НЕ запускались.

## 6. Принятые решения

Согласованы с владельцем, молча не переигрывать.

1. **JSON-пакет для переноса, книга — точечно.** Книга остаётся форматом содержания; в неё
   добавляется только лист «Исходы показателей».
2. **Полнота по построению.** Пакет сериализует строки таблиц целиком (основа —
   `TestSnapshotContent`), а не перечень полей. Перечень остался ровно в одном месте —
   списке таблиц теста, и он защищён гардом.
3. **Критерий приёмки — перенос в ЧИСТУЮ базу.** Круг «выгрузил-загрузил» на одной базе
   отменён как критерий: он проходит, даже если пакет не несёт ничего.
4. **Импорт сохраняет исходные идентификаторы.** Идентификатор — сам себе свидетельство
   происхождения. Перенумерация остаётся только для режима «создать копию».
5. **Пять частей в форме выбора**, режим upsert/replace только там, где возможна потеря.
6. **Режим для «Структуры и вопросов» задаётся ПО ТЕМЕ**, а не на всю часть.
7. **Права на темы проверяются на сервере.** В коде это обязательный порт `canManageTopic`
   у `applyTransfer` — роут не может забыть проверку.
8. Права на выгрузку пакета — `tests.read` (как у книги), не SCORM-скоуп.
9. **План пересчитывается на сервере**, а не принимается от клиента: увиденное автором есть
   основание согласия, а не список записей.

## 7. Грабли

1. **Круг на одной базе слеп к переносу.** Дефект виден только на настоящем приёмнике.
2. **`sort_order` контентных страниц — НЕ идентичность.** Импорт книгой оставляет все
   страницы с `sort_order = 0`. Сопоставлять страницы по `kind`, и только когда вид на
   приёмнике один.
3. **`content_hash` вопросов не годится как ключ.** В dev: 1334 вопроса, 990 с хешем, 812
   различных значений.
4. **Экран итогов и отчёт настраиваются в РАЗНЫХ местах.** `scalesChartKind`, `scoreSummary`,
   цвета шкал — в `content_pages.settings_json` страницы `results`; у отчёта — в
   `tests.report_settings_json`.
5. **Колонки называются не так, как кажется.** У вопроса это `prompt`, у шкалы `label`, у
   контентной страницы заголовка НЕТ вовсе. Фикстура, выдумавшая `text`/`title`, дала
   зелёные тесты при пустых подписях на настоящих строках — поймано только сверкой со
   схемой.
6. **Строка `tests` принадлежит ТРЁМ частям формы** (оценивание, итоги и оформление,
   остальное). Отсюда `omitFields` у операции: снятая часть удерживает свои колонки, всё
   прочее едет по умолчанию.
7. **`TaskStop` не убивает дочерний процесс.** После остановки фонового `npm run dev` порт
   остаётся занятым; освобождать через `Get-NetTCPConnection -LocalPort <порт>` и
   `Stop-Process -Force`.
8. **`psql` из Git Bash отдаёт cp1251** — нужен `PGCLIENTENCODING=UTF8`; кириллицу в
   запросах лучше заменять поиском по идентификатору. Вывод curl/python в консоль тоже
   искажает кириллицу — судить по кодам ответа и числам, а не по виду строк.
9. **Секреты прода не находятся по якорю.** `grep '^DATABASE_URL='` по
   `/srv/app/test_builder/env/.env` не находит строку — искать без `^`.
10. **Тема дизайн-системы в эскизах** задаётся классами `ou ou--dark ou--normal` на `body`,
    НЕ атрибутом `data-tone`.
11. **Прод-БД доступна по сети** (`192.168.1.200:5432`), туннель не нужен.

## 8. Следующий шаг

Задача 6 плана `docs/plans/2026-08-12-prd48-selective-import.md`: форма импорта в
`client/src/features/tests/transfer/`, пункт меню — рядом с экспортом книги в
`client/src/features/tests/list/tests-list.tsx`. Разметку брать ИЗ КОМПОНЕНТОВ ui-kit по
эскизу `.playwright-mcp/wf-transfer-import.html`, приёмка — в браузере, со скриншотом
каждого шага.

До начала работы стоит получить у владельца три решения: подтверждение четырёх отступлений
из задачи 3 плана, разрешение на полный `npm run test:it` и судьбу тест-копии
`f70eb383-28fe-4f51-abff-4bc4eb2013ac` в dev (она уже пережила применение плана — тест цел,
выгрузка совпадает с исходной).
