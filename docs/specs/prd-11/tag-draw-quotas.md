# PRD-11: Квоты выдачи по тегам

**Версия:** 0.1 (черновик на согласование)  
**Статус:** План — Стадия 0a (не реализовано); post-MVP, доставка (ортогонально скорингу)  
**Дата:** 2026-06-03  
**Связанные документы:** [BRD](../brd-scorm-enhancements.md) (BR-10),
[PRD-4](../prd-4/course-flow-sections.md) (темы теста, выдача),
[scoring-model.md](../scoring-model.md) §2.4 (стратифицированная выдача)  
**Этап BRD:** BR-10 (Этап 11)  
**Эталон-источник:** РТК-сертификация руководителей (покрытие подтем внутри раздела)  
**Зависимость:** `test_sections` + логика выдачи (PRD-4), `questions.tags` (PRD-2 §8.2)

## Терминология

- **Подтема** = значение в `questions.tags` (напр. `finance_base`). Отдельной сущности «подтема»
  нет; иерархии тем нет — переиспользуется существующее поле тегов.
- **Квота выдачи** = объём вопросов с заданным тегом ВНУТРИ общей выборки темы `drawCount`,
  с режимом `mode`: `exact` (РОВНО `count`) или `min` (НЕ МЕНЕЕ `count`). Режим задаётся ОБЩИЙ на
  тему (дефолт) либо ПО ТЕГАМ (`modeGranularity`). Остаток выборки — случайный из вопросов без
  `exact`-квота-тегов.

Это механизм **ДОСТАВКИ** (какие вопросы выдать обучающемуся), ортогональный и расчёту, и
«Цене ответа» (PRD-10). В скоринг не вмешивается.

## Статус реализации (на 2026-06-04)

**Стадии 1-3 выполнены.** Дизайн согласован: подтема = тег; опциональная квота на теме;
нехватка вопросов → warning.

- **Стадия 1 (схема):** миграция `011` (колонка `test_sections.draw_blueprint_json`, CHECK на
  `modeGranularity`/`mode`); zod `drawBlueprintSchema`/`drawStratumSchema` + колонка `drawBlueprintJson`;
  проброс в storage (`_insertSections` + legacy) и `SectionPayload`; валидация `Σ count <= drawCount`
  (FR-05) в `sectionBodySchema`; тесты `tests/schema-prd11-blueprint.test.ts`.
- **Стадия 2 (логика выдачи):** авторитетный движок `shared/draw/blueprint.ts` `drawSection(...)`
  (страты по порядку, общий `used` для дедупа FR-04, остаток без `exact`-тегов FR-03a, warning при
  нехватке FR-06, `shuffle` инъектируется); рукописный JS-порт в `server/scorm/assets/app.js`
  (`drawSection`), подключён в `generateVariant`; серверный отбор `server/routes/attempts.ts` использует
  авторитетный TS напрямую. Тесты `tests/draw-blueprint.test.ts` + golden-parity
  `tests/draw-blueprint-port.test.ts`.
- **Стадия 3 (экспорт):** `buildTestJson` переносит `drawBlueprint` в рантайм-секцию пакета УСЛОВНО
  (только когда задано → пакеты без квот бит-идентичны, FR-02). Тесты в `tests/scorm-builders.test.ts`.

Бэкенд квот работает end-to-end (схема → валидация → выдача в рантайме и на сервере → экспорт).
Дальше — **UI-стадия 4** (квота-редактор в теме), но сперва эскиз квота-редактора (Стадия 0b, ещё
не создан) + согласование (NFR-14, BR-08-10). Стадия 5 — приёмочные тесты выдачи (частично закрыты
unit/parity в Стадии 2).

---

## 1. Обзор

### 1.1 Контекст

Сейчас выдача вопросов на тему равномерно случайна: `shuffle(вопросы_темы).slice(0, drawCount)`
([app.js](../../../server/scorm/assets/app.js)). Гарантировать «N вопросов определённой подтемы
в форме» нельзя: отбор не различает подтемы/теги. Единственная стратификация — адаптивный режим
(`adaptive_levels`: квота по диапазону СЛОЖНОСТИ внутри темы), но это другая ось.

Ближайший потребитель — РТК-сертификация: внутри раздела (напр. «Финансы») есть подтемы
(`finance_base`/`finance_metrics`/`finance_invest`), и форма должна покрывать их в нужном объёме.

### 1.2 Метрики успеха

| Цель | Метрика |
| --- | --- |
| Гарантированное покрытие подтем | при заданной квоте каждая попытка содержит `count` (`exact`) либо не менее `count` (`min`) вопросов на тег (в общей выборке `drawCount`) |
| Обратная совместимость | тесты без квоты выдаются как сейчас (равномерно), без изменений |
| Прозрачность нехватки | нехватка вопросов с тегом видна автору и в выдаче (warning) |

---

## 2. Пользователи

- **Автор теста** — опционально задаёт на теме квоты `[тег → сколько]`; видит предупреждение,
  если вопросов с тегом меньше квоты.
- **Обучающийся** — получает выдачу с гарантированным покрытием подтем (либо равномерную, если
  квота не задана).

---

## 3. Функциональные требования

| ID | Требование | Основание |
| --- | --- | --- |
| FR-01 | На теме (`test_sections`) — опциональное `draw_blueprint_json.strata: [{ tag, count }]` | §2.4 scoring-model |
| FR-02 | Квота НЕ задана → текущая равномерная выдача `shuffle(all).slice(0, drawCount)` (без изменений) | BR-10, NFR-10 |
| FR-03 | Квота задана → на каждый тег по режиму `mode`: `exact` — РОВНО `count`, `min` — НЕ МЕНЕЕ `count` (случайный отбор внутри тега) | BR-10 |
| FR-03a | Остаток выборки до `drawCount` — случайный из вопросов без `exact`-квота-тегов (вопросы `min`-тегов и без квот в остатке участвуют) | BR-10 |
| FR-03b | Гранулярность режима `modeGranularity`: `uniform` (ОБЩИЙ режим на тему, дефолт) либо `per_tag` (режим в каждой страте); per-тег `mode` действует только при `per_tag` | BR-10 |
| FR-04 | Вопрос с несколькими квота-тегами засчитывается ОДИН раз (страты отбираются по порядку, общий `usedIds`) | — |
| FR-05 | `Σ count <= drawCount`; остаток `drawCount − Σ count` добирается случайно (по FR-03a); режим — общий на тему (дефолт) либо per страта, дефолт `exact` | решение 2026-06-04 |
| FR-06 | Нехватка вопросов с тегом (`доступно < count`) → WARNING (НЕ блокирует): в редакторе и в выдаче (выдаётся сколько есть) | решение 2026-06-03 |
| FR-07 | Классификация — `questions.tags` (существующее поле); новой сущности/иерархии тем нет | PRD-2 §8.2 |
| FR-08 | Ортогонально цене ответа (PRD-10) и расчёту; пороги/правила прохождения — на теме/тесте, не на теге | §11.7, §2.4 |

---

## 4. Модель данных

Без новых сущностей: переиспользуется `questions.tags` (классификация) и расширяется
`test_sections` одним опциональным `jsonb` (как уже `topic_pass_rule_json`/`feedback_json`).

```text
test_sections.draw_blueprint_json (опционально; отсутствие = равномерная выдача):
  modeGranularity: "uniform" | "per_tag"    # дефолт "uniform" (общий режим на тему)
  mode: "exact" | "min"                      # ОБЩИЙ режим (при modeGranularity="uniform"); дефолт "exact"
  strata: [ { tag: string, count: number, mode?: "exact" | "min" } ]   # count>=1; per-тег mode только при "per_tag"

  # эффективный режим страты: modeGranularity="uniform" -> blueprint.mode; "per_tag" -> strata[i].mode ?? "exact"

questions.tags: string[]                        # подтема = значение тега (уже есть)
```

Квоты — количества на тег (`exact` — ровно, `min` — не менее) внутри общей выборки `draw_count`
(`Σ count <= draw_count`); остаток добирается случайно из вопросов без `exact`-тегов (§5).

---

## 5. Логика выдачи

Расширение отбора ([app.js](../../../server/scorm/assets/app.js) и парного рантайма):

```text
если есть blueprint.strata:
  selected = []; used = {}
  effMode(s) = (modeGranularity == "uniform") ? blueprint.mode : (s.mode ?? "exact")
  exactTags = множество тегов страт, где effMode = "exact"
  для каждой страты {tag, count} по порядку:           # шаг 1: набрать count на тег
    pool = вопросы_темы.filter(q => !used[q.id] && q.tags.includes(tag))
    take = shuffle(pool).slice(0, count)               # take.length < count -> warning
    selected += take; пометить used
  # шаг 2: остаток до drawCount — из вопросов БЕЗ exact-тегов
  #        (mode=exact остаётся ровно count; mode=min может дорасти; untagged участвуют):
  free = вопросы_темы.filter(q => !used[q.id] && нет общих тегов с exactTags)
  selected += shuffle(free).slice(0, drawCount - selected.length)
  выдать selected                                        # |selected| <= drawCount
иначе:
  shuffle(вопросы_темы).slice(0, drawCount)              # как сейчас
```

- Дедупликация через общий `used` (вопрос в нескольких стратах — один раз, FR-04).
- Нехватка (`take.length < count`) → диагностический warning, выдаётся сколько есть (FR-06).
- Применяется в обоих местах отбора: рантайм SCORM-пакета и построение варианта попытки.

---

## 6. Валидация

- На сохранении темы: `Σ count <= draw_count` (квоты — минимумы внутри общей выборки; иначе
  ошибка валидации); на каждую страту `count <= число вопросов темы с этим тегом` — иначе
  **warning** (не блокирует, как баннеры покрытия в шкалах): «подтема `<tag>`: вопросов меньше
  квоты (`<доступно>` из `<count>`)».
- Пустой `strata` или `count < 1` — ошибка валидации.

---

## 7. UI (квота-редактор — отдельный эскиз + согласование)

Стартует ПОСЛЕ согласования эскиза (NFR-14). В теме (вкладка «Структура»):

- опциональный блок «Квоты по подтемам (тегам)»: переключатель гранулярности режима «Общий / По
  тегам» (дефолт «Общий»). При «Общий» — один SegmentedControl `exact`/`min` на тему; при «По
  тегам» — режим в каждой строке. Строки `[тег → сколько (→ режим при «По тегам»)]`: тег —
  Combobox/Tag, `count` — NumberInput, режим — SegmentedControl (дефолт `exact`);
- сумма квот vs `drawCount`; warning-баннер при нехватке вопросов с тегом;
- состояния: квота не задана (равномерная выдача), квота задана, ошибка, нехватка (warning).

Переиспользует чипы тегов и DS-примитивы; новых компонентов DS не требует.

---

## 8. Совместимость

- Поле `draw_blueprint_json` опционально; отсутствие = сегодняшняя равномерная выдача (NFR-10);
  старые тесты не трогаются.
- Адаптивный режим независим (стратификация по сложности через `adaptive_levels`); квоты по
  тегам — для стандартной (не-адаптивной) выдачи.

---

## 9. Вне охвата

- **«Свежесть на retry»** (anti-repeat — исключить вопросы прошлой попытки; ротация фиксированных
  форм РТК) — НЕ реализуема внутри автономного SCORM-пакета на WebTutor. ПОДТВЕРЖДЕНО 2026-06-04:
  WebTutor не поддерживает `adl.data` (Shared Data Buckets, `GetValue("adl.data._count")` → err 401),
  а `cmi.suspend_data` живёт только within-attempt → cross-attempt стора нет, пакет начинает новую
  попытку «слепым». Квота PRD-11 — stateless и от этого НЕ зависит. «Retry-different», если нужно,
  решается на уровне назначения/registration в LMS (вне пакета), либо descope. Память
  `reference_webtutor_scorm_runtime`; фиксированные формы РТК — issue #23.
- **Пороги/правила прохождения на уровне тега** (per-tag pass rule) — не вводятся; правила
  прохождения остаются на теме/тесте (РТК их на подтему не требует).
- **Стратификация по сложности** — уже есть в адаптивном режиме (`adaptive_levels`).

---

## 10. Этапы реализации

| Стадия | Содержание | Статус |
| --- | --- | --- |
| 0a | Этот PRD (план на согласование) | Выполнено |
| 0b | Эскиз квота-редактора в теме + согласование (DS-handbook first) | Ожидает (нужен для Стадии 4) |
| 1 | Схема `test_sections.draw_blueprint_json` + миграция + zod | Выполнено (2026-06-04, миграция `011`) |
| 2 | Логика выдачи (app.js + парный рантайм) с дедупликацией + warning при нехватке | Выполнено (2026-06-04, движок + порт + parity) |
| 3 | Экспорт блюпринта в SCORM-пакет (`test-json`) | Выполнено (2026-06-04, условно) |
| 4 | UI квота-редактора в теме (после согласования эскиза) | Ожидает (гейт: эскиз 0b) |
| 5 | Тесты: выдача по квотам, дедуп, нехватка→warning, обратная совместимость (нет квоты) | Ожидает |

---

## 11. Открытые вопросы

| # | Вопрос | Кандидат-решение |
| --- | --- | --- |
| OQ-1 | Остаток + режим + гранулярность | РЕШЕНО 2026-06-04: остаток случайный из вопросов без `exact`-тегов; режим `exact`/`min`; гранулярность `modeGranularity` = `uniform` (дефолт, общий) или `per_tag`; дефолт режима `exact` (FR-03/03a/03b/05, §5) |
| OQ-2 | Нужен ли отдельный namespace тегов-подтем (отличать от прочих тегов) | Пока НЕТ: блюпринт сам перечисляет теги-квоты; namespace ввести, если теги начнут смешиваться |
| OQ-3 | Порядок отбора при пересекающихся тегах влияет на состав | Зафиксирован порядок страт + общий `used` (FR-04); документировать для автора |
