# План реализации: владение контентом, целостность и модель публикации

**Версия:** 1.0
**Статус:** Согласован (2026-06-11)
**Дата актуализации:** 2026-06-11
**Связанные документы:** [BRD](../brd-content-ownership.md),
[PRD-15](./content-ownership.md),
[Аудит владения данными](../../AUDIT_DATA_OWNERSHIP.md),
[Ролевая модель и матрица](../access-control/role-model.md)

---

## 1. Принципы плана

- Источник истины по правам — `role-model.md`; изменения прав начинаются с правки этого файла.
- Каждая фаза самодостаточна: завершается зелёным `npm test` + `npm run check` и не меняет
  поведение для пользователей, пока соответствующая функциональность не включена целиком.
- Сначала чистые shared-модули (резолвер цены, логика выполнимости) с unit-тестами, затем данные,
  затем сервер, затем клиент.
- UI-задачи проходят этап эскизов и согласования до React-кода (правило wireframes-first);
  компоненты — только из `@universityrt/ui-kit`.
- Миграции аддитивны; единственное удаление (колонки оценки из `questions`) — отдельным релизом
  после подтверждения паритета golden-тестами.
- Серверная проверка — источник истины; клиент только скрывает недоступное.

---

## 2. Порядок и зависимости фаз

| Фаза | Содержание | Зависит от | Оценка |
| --- | --- | --- | --- |
| 0 | Подготовка: права в role-model.md, чистые shared-модули | — | 2-3 дня |
| 1 | Блок A: целостность | Фаза 0 | 4-8 дней |
| 2 | Блок B: публикация через снапшоты | Фаза 1 (publish-валидация) | 4-7 недель |
| 3 | Блок C: владение темами и гранты | Фаза 2 (отзыв опирается на снапшоты) | 2-3.5 недели |
| 4 | Блок D: оценка в тесте | Фаза 0; параллельна фазам 2-3 | 2.5-3.5 недели |
| 5 | Закрытие: сквозная приёмка, документация, RUNBOOK | Фазы 1-4 | 3-5 дней |

Суммарно последовательно 10-14 недель; с параллельным блоком D — ориентир 8-12 недель.
Жёсткая связка: финальный шаг блока D (удаление колонок) — только после фазы 5 предыдущего объёма.

---

## 3. Фазы и задачи

### Фаза 0. Подготовка

Цель: зафиксировать права и вынести чистую логику до изменений данных.

| Задача | Содержание | Артефакты | Покрывает |
| --- | --- | --- | --- |
| T-01 | Правка role-model.md: права контента «использовать/управлять», гранты тем, ветвь владельца для грантов тестов, матрица и реестр эндпоинтов | `docs/specs/access-control/role-model.md` | FR-29, BRC-03 - BRC-06, BRC-27 |
| T-02 | Чистый резолвер эффективной цены и сложности (цепочка переопределений, системное умолчание) + unit-тесты | `shared/scoring/effective-scoring.ts` | FR-31, FR-32 |
| T-03 | Чистая логика выполнимости выдачи (пул/квоты/уровни/шкалы от эффективного состояния) + unit-тесты | `shared/draw/feasibility.ts` | FR-04 |
| T-04 | Дополнение `canReadTest` ветвью «назначен пользователю» по role-model 6.4 + тесты | `server/services/test-access.ts` | FR-09 |

### Фаза 1. Блок A: целостность

Цель: ни одна разрушающая операция не деградирует чужой опубликованный тест молча.

| Задача | Содержание | Артефакты | Покрывает |
| --- | --- | --- | --- |
| T-05 | Миграция: `created_by` в 4 таблицах, индекс `test_sections.topic_id`; заполнение при создании | `shared/schema.ts`, `migrations/`, `server/storage.ts` | FR-01 |
| T-06 | DAL использования: `getTestsUsingTopic` / `getTestsUsingQuestion` | `server/storage.ts` | FR-03 |
| T-07 | Сервис выполнимости поверх T-03 + DAL; интеграция в одиночные и массовые пути тем/вопросов; ответ `409` со списком и причинами; политика published/draft; адм. переопределение | `server/services/draw-feasibility.ts`, `server/routes/topics.ts`, `questions.ts` | FR-02, FR-05 |
| T-08 | Интеграция проверки в импорт PRD-14 (dry-run показывает влияние) и в публикацию (`flow-policy-validator`) | `server/services/workbook-import.ts`, `flow-policy-validator.ts` | FR-05, FR-06 |
| T-09 | Гигиена `deleteTopic`: `topic_events`, зависимые `test_sections` | `server/storage.ts` | FR-07 |
| T-10 | Scope аналитики: агрегаты, экспорты, attempt-by-id, scorm-агрегаты через `readableTestScope` / `canReadTestAnalytics` | `server/routes/analytics/*` | FR-08 |
| T-11 | Закрытие `GET /tests/:id/design` и `/screen-template/:screen` объектной проверкой (после T-04) | `server/routes/tests.ts` | FR-09 |
| T-12 | Клиент: диалог отказа `409` со списком зависимых тестов и причин (эскиз -> согласование -> код) | `client/src/pages/author/*` | FR-05 |
| T-13 | Приёмка фазы: сценарии E-1 - E-6, E-9, E-10, E-12 | `tests/` | BRC-14 - BRC-16 |

### Фаза 2. Блок B: публикация через снапшоты

Цель: опубликованный тест неизменен до переопубликации; попытки доигрываются на своей версии.

| Задача | Содержание | Артефакты | Покрывает |
| --- | --- | --- | --- |
| T-14 | Таблица `test_snapshots` + сборщик снапшота с разрешёнными значениями (через резолвер T-02) | `shared/schema.ts`, `migrations/`, `server/services/test-snapshot.ts` | FR-10 |
| T-15 | Публикация/переопубликация создают снапшот; проверка выполнимости при публикации (из T-08) | `server/routes/tests.ts` | FR-10, FR-06 |
| T-16 | Доставка из снапшота: старт, выдача, ответы, завершение, восстановление; пин `attempts.testVersion`; живой предпросмотр черновика | `server/routes/attempts.ts`, `server/services/result-compute.ts` | FR-11, FR-13 |
| T-17 | Адаптивный режим из снапшота (уровни и эффективная сложность) | `server/routes/attempts.ts`, `shared/draw/` | FR-11 |
| T-18 | Признак изменений: правки теста + дрейф пулов по `contentHash`; вычисление при открытии редактора и списка | `server/services/test-snapshot.ts`, `server/routes/tests.ts` | FR-12 |
| T-19 | Экстренная переопубликация: эндпоинт, аннулирование попыток без списания лимита PRD-6, уведомления, журнал | `server/routes/tests.ts`, `server/services/retake-gate.ts` | FR-14 |
| T-20 | Аналитика по версиям; вопросные статистики из снапшота | `server/routes/analytics/*` | FR-15 |
| T-21 | Экспорт SCORM из актуального снапшота; локальная проверка плеером | `server/scorm/builders/test-json.ts`, `npm run scorm:player` | FR-16 |
| T-22 | Удержание и очистка снапшотов; сжатие `content_json` | `server/services/test-snapshot.ts` | FR-17 |
| T-23 | Клиент: бейджи состояний, действие «Опубликовать изменения», индикатор дрейфа, диалог экстренной переопубликации (эскизы -> согласование -> код) | `client/src/pages/author/tests.tsx`, `client/src/features/tests/editor/` | FR-12, FR-14 |
| T-24 | Приёмка фазы: E-7, E-8; доигрывание при переопубликации; экстренный сценарий с PRD-6 | `tests/` | BRC-20 - BRC-24 |

### Фаза 3. Блок C: владение темами и гранты

Цель: видимость тем по владению и грантам; единая модель выдачи «владелец + администратор».

| Задача | Содержание | Артефакты | Покрывает |
| --- | --- | --- | --- |
| T-25 | Миграция: `owner_id`, `visibility` (legacy: NULL + shared), таблица `topic_access_grants` (user/group, use/manage, state) | `shared/schema.ts`, `migrations/` | FR-18, FR-19 |
| T-26 | Сервис области видимости тем (`visibleTopicScope`: свои, гранты личные и групповые, общие) + DAL грантов | `server/services/topic-access.ts`, `server/storage.ts` | FR-22 |
| T-27 | Эндпоинты грантов темы (владелец и администратор), смена владельца, журналирование; ветвь владельца в `canGrantTestAccess` (унификация тестов) | `server/routes/topics.ts`, `tests.ts`, `server/services/test-access.ts` | FR-20, FR-21 |
| T-28 | Фильтрация списков и экспорта тем/вопросов; валидация секций при сохранении теста; инвариант доставки (контентные права не проверяются в попытках и SCORM) | `server/routes/topics.ts`, `questions.ts`, `tests.ts` | FR-22 - FR-24 |
| T-29 | Отзыв: мягкий режим (`revoked_in_use`, производное чтение «тема в контексте моего теста»), жёсткий через зависимости (сервис выполнимости) с адм. переопределением и журналом | `server/services/topic-access.ts` | FR-25, FR-26 |
| T-30 | Одноимённость: нормализация, предупреждение в видимой области, частичный уникальный индекс по владельцу, админ-отчёт о дублях | `server/routes/topics.ts`, `migrations/` | FR-27 |
| T-31 | Импорт PRD-14: сопоставление тем в видимой области, владелец-импортёр для новых; экспорт в видимой области; скрытие пустых папок | `server/services/workbook-import.ts`, `server/routes/folders.ts` | FR-28 |
| T-32 | Клиент: фильтры «мои/доступные/общие», признак видимости, управление грантами темы, предупреждение об одноимённости, индикатор недоступной темы (эскизы -> согласование -> код) | `client/src/pages/author/topics.tsx` и смежные | FR-22, FR-25, FR-27 |
| T-33 | Приёмка фазы: E-11, E-13; конфиденциальность ключей (F-10); унификация грантов тестов | `tests/` | BRC-01 - BRC-13, BRC-27 |

### Фаза 4. Блок D: оценка в тесте (параллельна фазам 2-3)

Цель: цена и градуировка — свойство теста; вопрос — только контент.

| Задача | Содержание | Артефакты | Покрывает |
| --- | --- | --- | --- |
| T-34 | Миграция: `test_question_scoring`, `default_points` на тесте и секции | `shared/schema.ts`, `migrations/` | FR-30, FR-31 |
| T-35 | Подключение резолвера T-02: веб-оценивание, серверный пересчёт, сборка SCORM, снапшоты | `server/utils/check-answer.ts`, `server/services/result-compute.ts`, `server/scorm/builders/test-json.ts` | FR-32 |
| T-36 | Переопределение сложности в адаптивной выдаче (эффективная сложность) | `shared/draw/`, `server/routes/attempts.ts` | FR-34 |
| T-37 | Backfill отличающихся значений; прогон golden-тестов скоринга; сверка пересчёта попыток | `migrations/`, `tests/mbi-golden.test.ts` | FR-33, BRC-26 |
| T-38 | Книга Excel: колонки оценки в test-scoped лист; шаблон, инспекция, round-trip | `server/utils/workbook-sheets.ts`, `server/services/workbook-import.ts` | FR-36 |
| T-39 | Клиент: умолчания и переопределения в редакторе теста, индикатор устаревших, перенос конструктора градуировки из карточки вопроса (эскизы -> согласование -> код) | `client/src/features/tests/editor/`, `client/src/pages/author/questions.tsx`, `scoring-builder.tsx` | FR-35 |
| T-40 | Отдельный релиз: удаление `questions.points` и `questions.scoring_json` после паритета | `migrations/`, `shared/schema.ts` | FR-33 |

### Фаза 5. Закрытие

| Задача | Содержание | Артефакты | Покрывает |
| --- | --- | --- | --- |
| T-41 | Сквозная приёмка: вся матрица E-1 - E-13, инвариант доставки, SCORM локально (`scorm:sample` + `scorm:player`) | `tests/`, `out/` | Раздел 8 PRD |
| T-42 | Матрица приёмки трека (по образцу acceptance-matrix.md PRD-13) | `docs/specs/access-control/` или `docs/specs/prd-15/` | — |
| T-43 | RUNBOOK развёртывания: порядок миграций, переходный режим снапшотов, откат | `docs/RUNBOOK_prd15_content_ownership.md` | — |
| T-44 | Актуализация CLAUDE.md, ROADMAP, закрытие отложенных пунктов acceptance-matrix PRD-13 (аналитика) | `CLAUDE.md`, `docs/ROADMAP.md` | FR-08 |

---

## 4. Контрольные точки согласования

- После фазы 0: правки role-model.md утверждены до любого кода прав.
- Перед T-12, T-23, T-32, T-39: эскизы UI согласованы (wireframes-first).
- Перед T-40: явное подтверждение паритета (golden + сверка пересчёта) и отдельное согласование
  удаления колонок.
- Экстренная переопубликация (T-19): сценарий и формулировки уведомлений согласовать отдельно.

---

## 5. Риски плана

| Риск | Реакция |
| --- | --- |
| Снапшоты затрагивают весь рантайм попыток (самая дорогая фаза) | Переходный режим: тесты без снапшота играют по-старому; перевод по одному действию публикации |
| Дрейф пулов считается дорого на больших банках | Вычисление по запросу (открытие редактора/списка), кэш по `contentHash` |
| Параллельность блока D с активным треком PRD-14 | T-38 согласуется с владельцем трека PRD-14 до начала; общие файлы — `workbook-*` |
| Производное чтение (мягкий отзыв) расширяет поверхность доступа | Реализация строго в контексте теста получателя; отдельные тесты на отсутствие доступа к банку |
| Удаление колонок ломает старые ветки/скрипты | Отдельный релиз, объявление в команде, поиск использований перед T-40 |

---

## 6. Критерии готовности трека

- Все BRC-01 - BRC-27 покрыты реализованными FR и приёмочными проверками.
- Матрица E-1 - E-13: каждый сценарий воспроизведён и предотвращён автоматическим тестом.
- `npm test`, `npm run check`, `npm run lint:md` зелёные; golden-тесты скоринга без расхождений.
- Поведение системы в день включения каждой миграции не изменилось (проверка на копии данных).
- Документация актуальна: role-model.md, RUNBOOK, ROADMAP, матрица приёмки.
