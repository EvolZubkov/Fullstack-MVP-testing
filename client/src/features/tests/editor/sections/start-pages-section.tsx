/**
 * @module features/tests/editor/sections/start-pages-section
 * @description Editor section that manages start content pages
 * (`content_pages` of type `intro` without `topic_id`). Hosts the legacy
 * `start_page_content` migration banner. Implemented in phase 5D.
 */

export type StartPagesSectionProps = {
  testId?: string;
};

export function StartPagesSection(
  _props: StartPagesSectionProps,
): JSX.Element | null {
  return null;
}
