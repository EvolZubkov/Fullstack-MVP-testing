/**
 * @module features/tests/editor/sections/basic-settings-section
 * @description Editor section for basic test fields: title, description,
 * status, feedback, telemetry, webhook URL. Reference section implemented
 * in phase 4B.
 */

export type BasicSettingsSectionProps = {
  testId?: string;
};

export function BasicSettingsSection(
  _props: BasicSettingsSectionProps,
): JSX.Element | null {
  return null;
}
