/**
 * @module features/tests/editor/sections/adaptive-settings-section
 * @description Editor section for adaptive test settings: per-topic levels,
 * difficulty ranges, level links. Hidden when `model.mode !== "adaptive"`.
 * Implemented in phase 5C.
 */

export type AdaptiveSettingsSectionProps = {
  testId?: string;
};

export function AdaptiveSettingsSection(
  _props: AdaptiveSettingsSectionProps,
): JSX.Element | null {
  return null;
}
