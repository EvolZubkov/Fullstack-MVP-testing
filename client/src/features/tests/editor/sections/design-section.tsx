/**
 * @module features/tests/editor/sections/design-section
 * @description Editor section for the "Оформление" tab. Reuses the existing
 * design-settings-dialog body inline; `templateVersion` and
 * `templateApiVersion` are read-only. Implemented in phase 5E.
 */

export type DesignSectionProps = {
  testId?: string;
};

export function DesignSection(
  _props: DesignSectionProps,
): JSX.Element | null {
  return null;
}
