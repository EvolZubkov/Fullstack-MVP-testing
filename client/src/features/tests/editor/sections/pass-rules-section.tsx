/**
 * @module features/tests/editor/sections/pass-rules-section
 * @description Editor section for `passDecisionPolicy`, the overall pass rule
 * and per-topic pass rules. Implemented in phase 5B.
 */

export type PassRulesSectionProps = {
  testId?: string;
};

export function PassRulesSection(
  _props: PassRulesSectionProps,
): JSX.Element | null {
  return null;
}
